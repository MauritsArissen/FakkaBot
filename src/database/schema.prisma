generator client {
  provider = "prisma-client-js"
}

model UserStats {
  uid String @id
  xp  Int
}

model Activity {
  id        Int      @id @default(autoincrement())
  uid       String
  timestamp DateTime @default(now())
  level     Int
}
