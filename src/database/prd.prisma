datasource db {
  provider = "postgresql"
  url      = "env(DATABASE_URL)"
}

generator client {
  provider = "prisma-client-js"
}

model Logs {
  id        Int      @id @default(autoincrement())
  type      String?
  message   String?
  payload   String?
  timestamp DateTime
}

model UserStats {
  uid String @id
  xp  Int
}
